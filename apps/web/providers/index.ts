// Placeholder for future Convex/Inkeep providers
// No runtime dependency for now

export interface ConvexProvider {
  // Future Convex integration
}

export interface InkeepProvider {
  // Future Inkeep integration
}

// Export empty placeholders
export const convexProvider: ConvexProvider = {};
export const inkeepProvider: InkeepProvider = {};
